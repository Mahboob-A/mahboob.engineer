/**
 * components/diagrams/MovioMiniDiagram.tsx
 *
 * Phase 6 (T7): the landing's Projects section now reuses the full
 * `MovioDiagram` (12 nodes, ~16 edges, animated packet flow on the
 * upload + delivery paths). The standalone mini variant is gone.
 */

export { MovioDiagram as MovioMiniDiagram } from "./MovioDiagram";